/node_modules
/.next
/out
/build
.DS_Store
*.pem
.env
.env*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo
next-env.d.ts

# booked meetings written by `npm run dev` — server state, never source
/data

# files deploy.sh moved aside because git started tracking them
/.deploy-backup

# deploy state (server-side, not source)
/nginx/active/*
!/nginx/active/.gitkeep
/certbot

# public/media IS committed — the server has no scp path, so the video ships in the repo

# old video cuts kept around locally; GitHub warns past 50MB and they bloat every clone
*.PREVIOUS_*.mp4

# build artifact of scripts/upload.sh
/swenlly-deploy.tar.gz
