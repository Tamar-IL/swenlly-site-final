/node_modules
/.next
/out
/build
.DS_Store
*.pem
.env
.env*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo
next-env.d.ts

# deploy state (server-side, not source)
/nginx/active/*
!/nginx/active/.gitkeep
/certbot

# large media — rsynced to the server, bind-mounted into the container
/public/media

# old video cuts kept around locally; GitHub warns past 50MB and they bloat every clone
*.PREVIOUS_*.mp4

# build artifact of scripts/upload.sh
/swenlly-deploy.tar.gz
