# Shell scripts and Docker/nginx configs must keep LF endings — CRLF makes Linux
# fail them with "bad interpreter: /usr/bin/env bash^M".
*.sh        text eol=lf
Dockerfile  text eol=lf
*.conf      text eol=lf
*.yml       text eol=lf
